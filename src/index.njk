---
layout: layouts/base.njk
title: 首页
eleventyNavigation:
  key: 首页
  order: 1
---

<div>
	{% for post in collections.posts %}
    {% if post.data.thumbnail %}
      <div class="flex bg-white ring-1 ring-gray-100 ring-offset-0 shadow-md rounded-md mb-5">
        <div class="w-2/3 pt-8 pb-8 pl-8 pr-4">
          <div class="text-xl font-bold" ><a class="hover:text-gray-500" href="{{ post.url }}">{{ post.data.title }}</a></div>
          {# <h1 class="text-2xl font-bold">{{ post.data.title }}</h1> #}
          <p class="mt-2 text-sm">L2TP是一种工业标准的Internet隧道协议，功能大致和PPTP协议类似 ...</p>
          <p class="mt-2 text-xs text-gray-400">
            {{ post.data.date | formaterDate }}
          </p>
        </div>
        <div class="w-1/3">
          <img src="{{post.data.thumbnail}}" alt="Image Thumbnail" class="w-full h-full rounded-r-md">
        </div>  
      </div>
    {% else %}
     <div class="flex bg-white ring-1 ring-gray-100 ring-offset-0 shadow-md rounded-md mb-5">
        <div class="p-8">
          <div class="text-xl font-bold" ><a class="hover:text-gray-500" href="{{ post.url }}">{{ post.data.title }}</a></div>
          <p class="mt-2 text-sm">L2TP是一种工业标准的Internet隧道协议，功能大致和PPTP协议类似 ...</p>
          <p class="mt-2 text-xs text-gray-400">
            {{ post.data.date | formaterDate }}
          </p>
        </div>
      </div>
    {% endif %}
	{% endfor %}
</div>

